# Описание проекта «Торгуй сам»

## 1. Общие сведения о проекте

**Название проекта:** «Торгуй сам» (внутреннее имя пакета: `next-commerce-tailwind`)

**Тип приложения:** Веб-приложение — онлайн-маркетплейс (доска объявлений) для размещения и поиска товаров, аналог Avito/Юла.

**Назначение:** Платформа предназначена для размещения объявлений о продаже товаров частными лицами, индивидуальными предпринимателями и юридическими лицами (ООО). Пользователи могут публиковать товары, искать и фильтровать объявления, вести переписку с продавцами в реальном времени, оставлять отзывы, управлять балансом и продвигать свои объявления.

**Версия:** 0.1.0 (стадия активной разработки)

**Локализация:** Русский язык (интерфейс полностью на русском, поддержка российских номеров телефона в формате +7)

---

## 2. Технологический стек

### 2.1. Фреймворк и язык программирования

| Технология        | Версия           | Назначение                                                           |
| ----------------- | ---------------- | -------------------------------------------------------------------- |
| **Next.js**       | 16.0.1           | Основной фреймворк для серверного и клиентского рендеринга (SSR/CSR) |
| **React**         | 19.2.0           | Библиотека для построения пользовательского интерфейса               |
| **TypeScript**    | 5.9.3            | Статически типизированный язык программирования                      |
| **Node.js / Bun** | Bun 1.3 (Docker) | Среда выполнения JavaScript на сервере                               |

### 2.2. Стилизация и UI-компоненты

| Технология                         | Версия             | Назначение                                                                         |
| ---------------------------------- | ------------------ | ---------------------------------------------------------------------------------- |
| **Tailwind CSS**                   | 4.1.17             | Утилитарный CSS-фреймворк для стилизации                                           |
| **CSS Modules**                    | встроено в Next.js | Модульные CSS-стили для компонентов                                                |
| **shadcn/ui** (New York стиль)     | —                  | Библиотека переиспользуемых UI-компонентов на базе Radix UI                        |
| **Radix UI**                       | различные          | Набор доступных (a11y) headless-компонентов (Dialog, Select, Avatar, Toggle, Slot) |
| **class-variance-authority (CVA)** | 0.7.1              | Типизированное управление вариантами CSS-классов компонентов                       |
| **clsx**                           | 2.1.1              | Условная конкатенация CSS-классов                                                  |
| **tailwind-merge**                 | 3.4.0              | Интеллектуальное объединение Tailwind-классов без конфликтов                       |
| **tw-animate-css**                 | 1.4.0              | CSS-анимации для Tailwind                                                          |
| **Lucide React**                   | 0.553.0            | Библиотека SVG-иконок                                                              |
| **Шрифт Montserrat**               | Google Fonts       | Основной шрифт приложения (подключён через `next/font`)                            |

### 2.3. Управление состоянием и получение данных

| Технология               | Версия           | Назначение                                                                        |
| ------------------------ | ---------------- | --------------------------------------------------------------------------------- |
| **TanStack React Query** | 5.90.10          | Управление серверным состоянием, кэширование, синхронизация данных                |
| **React Context API**    | встроено в React | Глобальное состояние аутентификации (`AuthContext`) и чатов (`ChatSocketContext`) |
| **nuqs**                 | 2.8.5            | Типобезопасная синхронизация состояния с URL query-параметрами                    |
| **ofetch**               | 1.5.1            | HTTP-клиент для выполнения запросов к REST API бэкенда                            |

### 2.4. Формы и валидация

| Технология              | Версия | Назначение                                               |
| ----------------------- | ------ | -------------------------------------------------------- |
| **React Hook Form**     | 7.66.0 | Управление формами с минимальным количеством ре-рендеров |
| **Zod**                 | 4.1.12 | Декларативная валидация данных форм и типов              |
| **@hookform/resolvers** | 5.2.2  | Интеграция Zod-схем с React Hook Form                    |

### 2.5. Карты и геолокация

| Технология        | Версия | Назначение                    |
| ----------------- | ------ | ----------------------------- |
| **Leaflet**       | 1.9.4  | Библиотека интерактивных карт |
| **React Leaflet** | 5.0.0  | React-обёртка для Leaflet     |

### 2.6. Реалtime-коммуникации

| Технология                | Версия     | Назначение                                   |
| ------------------------- | ---------- | -------------------------------------------- |
| **Socket.IO Client**      | 4.8.1      | WebSocket-клиент для чата в реальном времени |
| **Web Notifications API** | браузерное | Push-уведомления о новых сообщениях          |

### 2.7. Уведомления

| Технология | Версия | Назначение                                |
| ---------- | ------ | ----------------------------------------- |
| **Sonner** | 2.0.7  | Toast-уведомления (всплывающие сообщения) |

### 2.8. Темизация

| Технология      | Версия | Назначение                                                            |
| --------------- | ------ | --------------------------------------------------------------------- |
| **next-themes** | 0.4.6  | Поддержка светлой/тёмной темы (подготовлена, но пока не активирована) |

### 2.9. Инструменты разработки

| Технология                        | Версия | Назначение                                 |
| --------------------------------- | ------ | ------------------------------------------ |
| **ESLint**                        | 9.39.1 | Статический анализ кода                    |
| **@antfu/eslint-config**          | 6.2.0  | Пресет конфигурации линтинга от Энтони Фу  |
| **@eslint-react/eslint-plugin**   | 2.3.13 | React-специфичные правила линтинга         |
| **@tanstack/eslint-plugin-query** | 5.91.2 | Правила линтинга для React Query           |
| **Prettier**                      | 3.6.2  | Форматирование кода                        |
| **prettier-plugin-tailwindcss**   | 0.7.1  | Автоматическая сортировка Tailwind-классов |
| **React Query Devtools**          | 5.90.2 | Инструменты отладки React Query            |
| **bumpp**                         | 10.3.1 | Утилита для управления версиями            |

### 2.10. Контейнеризация и деплой

| Технология                      | Назначение                                                                |
| ------------------------------- | ------------------------------------------------------------------------- |
| **Docker**                      | Контейнеризация приложения                                                |
| **Docker Compose**              | Оркестрация контейнеров                                                   |
| **Bun**                         | Runtime для сборки и запуска в production                                 |
| **Standalone output** (Next.js) | Оптимизированная standalone-сборка для минимального размера Docker-образа |

**Dockerfile:** Многоступенчатая сборка (multi-stage build):

1. **base** — базовый образ `oven/bun:1.3`
2. **deps** — установка зависимостей (`bun install --frozen-file`)
3. **builder** — сборка Next.js приложения (`bun run build`)
4. **runner** — production-образ с минимальным набором файлов

**Docker Compose:** Один сервис `next-app`, порт 3001 → 3000, переменные из `.env`, автоматический перезапуск.

---

## 3. Архитектура приложения

### 3.1. Общая архитектура

Приложение построено по клиент-серверной архитектуре:

- **Frontend (данный проект)** — Next.js 16 приложение с использованием App Router, гибридный рендеринг (SSR + CSR)
- **Backend (внешний сервис)** — REST API, к которому фронтенд обращается через HTTP-клиент `ofetch`. URL бэкенда задаётся переменной окружения `NEXT_PUBLIC_API_URL`
- **WebSocket-сервер** — для realtime-чатов, URL задаётся переменной `NEXT_PUBLIC_API_WS_URL`

### 3.2. Архитектура маршрутизации (App Router)

Проект использует **Next.js App Router** с типизированными маршрутами (`typedRoutes: true`).

```
src/app/
├── layout.tsx                    # Корневой layout (шрифт, метаданные, SSR-prefetch текущего пользователя)
├── providers.tsx                 # Клиентские провайдеры (QueryClient, Auth, ChatSocket, Nuqs)
├── globals.css                   # Глобальные стили и CSS-переменные темы
├── not-found.tsx                 # Страница 404
│
├── _components/                  # Общие компоненты layout
│   └── main-layout/             # Header + Footer + Toaster
│       ├── header/              # Шапка с логотипом, поиском, навигацией
│       └── footer/              # Подвал сайта
│
├── (feed)/                       # Route Group — публичная часть (лента товаров)
│   ├── (search)/                # Route Group — поиск и каталог
│   │   ├── page.tsx             # Главная страница (лента товаров)
│   │   ├── layout.tsx           # Layout с поиском, фильтрами, выбором региона
│   │   ├── [categorySlug]/      # Страница категории
│   │   │   └── [subcategorySlug]/ # Подкатегория → тип
│   │   ├── _features/           # Feature-модули (feed, category, ad, favorites, product)
│   │   ├── _components/         # Компоненты фильтров и региона
│   │   └── _lib/                # Утилиты модуля поиска
│   │
│   ├── product/[productId]/     # Страница товара (SSR)
│   └── seller/[sellerId]/       # Страница профиля продавца (SSR)
│
├── profile/                      # Личный кабинет пользователя (защищённый маршрут)
│   ├── layout.tsx               # Layout с боковым меню
│   ├── create-product/          # Создание объявления
│   ├── my-products/             # Мои объявления
│   │   └── [productId]/         # Редактирование товара
│   ├── messages/                # Список чатов / сообщения
│   │   ├── support/             # Чат технической поддержки
│   │   └── [chatId]/            # Чат с конкретным пользователем
│   ├── favorites/               # Избранные товары
│   ├── settings/                # Настройки профиля (ФИО, фото, тип профиля)
│   ├── balance/                 # Баланс и пополнение
│   ├── analytics/               # Аналитика продаж
│   ├── banner-request/          # Заявка на размещение баннера
│   ├── banner-stats/            # Статистика рекламных баннеров
│   └── promotion-request/       # Продвижение товаров
│
├── admin/                        # Панель администратора (защищённый маршрут)
│   ├── layout.tsx               # Layout с боковой панелью + проверка isAdmin
│   ├── page.tsx                 # Редирект на /admin/moderation
│   ├── moderation/              # Модерация товаров (одобрение/отклонение)
│   ├── reviews/                 # Модерация отзывов
│   ├── categories/              # Управление категориями/подкатегориями/типами/характеристиками
│   ├── users/                   # Управление пользователями (поиск, бан, удаление)
│   ├── logs/                    # Системные логи действий
│   ├── advertising/             # Управление рекламными баннерами
│   ├── promotion/               # Управление типами продвижения
│   └── support/                 # Чаты техподдержки с пользователями
│
└── _temp/                        # Временные/экспериментальные страницы
```

### 3.3. Защита маршрутов (Middleware)

Файл `middleware.ts` реализует серверную защиту маршрутов:

- **Защищённые префиксы:** `/profile`, `/admin`
- **Механизм:** Проверка наличия cookie `session_id`
- **Поведение при отсутствии сессии:** Редирект на `/` с query-параметром `?auth=1` (для автоматического открытия диалога авторизации)

Дополнительная проверка для администраторов: в `admin/layout.tsx` выполняется серверный запрос `checkIsAdmin()` к API, и при отсутствии прав доступа пользователь перенаправляется на страницу 404.

### 3.4. Архитектура слоя данных (API Layer)

```
src/lib/api/
├── instance.ts          # Конфигурация HTTP-клиента (ofetch)
├── index.ts             # Реэкспорт всех модулей
│
├── requests/            # Функции запросов к REST API
│   ├── auth/            # Аутентификация (login, register, verify, recover, checkIsAdmin)
│   ├── product/         # CRUD товаров, поиск, модерация, фильтры
│   ├── category/        # Получение категорий, подкатегорий, типов
│   ├── user/            # Управление пользователями (CRUD, бан, баланс)
│   ├── chat.ts          # Чаты (start, getAll, getById, getMessages)
│   ├── favorites/       # Избранное (add, remove, getAll)
│   ├── payment/         # Платежи (create, check status, history)
│   ├── banner/          # Баннеры (CRUD, статистика, трекинг просмотров)
│   ├── promotion/       # Продвижение товаров
│   ├── analytic/        # Аналитика
│   ├── reviews.ts       # Отзывы (создание, получение, модерация)
│   ├── address/         # Геокодирование (подсказки адреса, определение города по IP)
│   └── log/             # Системные логи
│
├── hooks/               # React Query хуки
│   ├── queries/         # Хуки запросов (useCurrentUser, useCategories, useChats, useAvailableFilters, и др.)
│   └── mutations/       # Хуки мутаций (useLoginMutation, useCreateProductMutation, useFavoriteMutation, и др.)
│
└── types/               # Типы запросов/ответов API
    ├── auth.ts          # Zod-схемы и типы авторизации
    ├── product.ts       # Zod-схемы создания/обновления товара
    ├── banner.ts        # Типы баннеров
    └── review.ts        # Типы отзывов и модерации
```

**HTTP-клиент (ofetch):**

- Базовый URL из переменной окружения `NEXT_PUBLIC_API_URL`
- Автоматическая отправка cookies (`credentials: "include"`)
- SSR-поддержка: на сервере cookies пробрасываются из `next/headers`
- При ответе 401 на клиенте: автоматическая очистка cookie `session_id` и сброс кэша текущего пользователя

### 3.5. Провайдеры и контексты

Цепочка провайдеров в `providers.tsx`:

```
NuqsAdapter (синхронизация URL-состояния)
  └── QueryClientProvider (TanStack React Query)
       └── HydrationBoundary (SSR-гидрация данных)
            └── AuthProvider (контекст авторизации)
                 └── ChatSocketProvider (WebSocket для чатов)
```

**AuthProvider:**

- Выполняет запрос текущего пользователя через React Query
- Предоставляет объект `user` и функцию `logout`
- `logout` очищает cookie и сбрасывает кэш

**ChatSocketProvider:**

- Подключается к WebSocket-серверу при наличии авторизованного пользователя
- Обрабатывает входящие сообщения, обновляет список чатов
- Показывает push-уведомления через Web Notifications API
- Предоставляет объект `socket` и флаг `isConnected`

---

## 4. Функциональные модули

### 4.1. Модуль аутентификации

**Расположение:** `src/components/auth-dialog/`

**Механизм:** Сессионная аутентификация на основе cookie `session_id`.

**Экраны:**

1. **Вход (LoginScreen)** — авторизация по логину (email/телефон) и паролю
2. **Регистрация (RegisterScreen)** — регистрация с указанием ФИО, email, телефона и пароля. Поддержка верификации через SMS или Telegram
3. **Подтверждение кода (VerifyCodeScreen)** — ввод 6-значного SMS-кода для верификации номера телефона
4. **Восстановление пароля (RecoverScreen)** — восстановление доступа по email (отправка кода, ввод кода, смена пароля)

**Валидация форм:**

- Zod-схемы: `loginSchema`, `registerSchema`, `verifyMobileCodeSchema`, `forgotPasswordSchema`
- Валидация российских номеров в формате +7 (XXX) XXX-XX-XX
- Минимальная длина пароля — 6 символов

**API-эндпоинты:**

- `POST /auth/sign-in` — авторизация
- `POST /auth/sign-up?where=sms|telegram` — регистрация
- `POST /auth/verify-mobile-code` — подтверждение SMS-кода
- `POST /auth/forgot-password` — запрос на восстановление
- `POST /auth/verify-code` — подтверждение кода восстановления
- `POST /auth/change-password` — смена пароля
- `GET /auth/me` — получение текущего пользователя
- `GET /auth/isAdmin` — проверка прав администратора

### 4.2. Модуль каталога и поиска товаров

**Расположение:** `src/app/(feed)/(search)/`

**Главная страница** отображает ленту всех товаров с возможностью фильтрации, поиска и навигации по категориям.

**Иерархия каталога:**

1. **Категория** (например, «Электроника»)
2. **Подкатегория** (например, «Телефоны»)
3. **Тип** (например, «Смартфоны»)
4. **Характеристики типа** (например, «Объём памяти», «Цвет»)

**Поддержка двух подходов к фильтрации:**

- **ID-based** (числовые ID категорий)
- **Slug-based** (читаемые slug'и для SEO-дружественных URL, например `/elektronika/telefony/smartfony`)

**Параметры фильтрации:**

- Категория / подкатегория / тип (по slug или ID)
- Диапазон цены (minPrice, maxPrice)
- Состояние товара: NEW (новый) / USED (б/у)
- Тип продавца: INDIVIDUAL (физ. лицо) / IP / ООО
- Рейтинг продавца (minRating, maxRating, 1–5)
- Динамические характеристики (fieldValues — запись ключ-значение)
- Текстовый поиск (search)
- Регион
- Сортировка: по дате, по цене, по релевантности
- Пагинация (page, limit)

**Автоматическое динамическое получение доступных фильтров:**

- Эндпоинт `GET /product/available-filters` возвращает только актуальные значения (диапазон цен, состояния, типы продавцов, поля характеристик)

**Компоненты поиска:**

- `SearchBox` — поле текстового поиска
- `CategoryDialog` — диалог выбора категории
- `FiltersButton` — кнопка открытия панели фильтров
- `RegionButton` — выбор региона
- `Filters` — панель фильтров с динамическими полями

### 4.3. Модуль карточки товара

**Расположение:** `src/components/product-card/`

**ProductCard** — компонент карточки товара в ленте:

- Превью изображений (с поддержкой множественных фото)
- Название, адрес, дата создания, цена (форматирование в рублях)
- Кнопка добавления/удаления из избранного (LikeButton)
- Навигация на страницу товара

**MyProductCard** — карточка товара для личного кабинета (с дополнительными действиями: скрыть/показать, удалить, редактировать)

### 4.4. Детальная страница товара

**Расположение:** `src/app/(feed)/product/[productId]/`

**Рендеринг:** Серверный (SSR) — данные загружаются на сервере через `getProductById`.

**Функциональность:**

- Хлебные крошки навигации (Breadcrumb): Главная → Категория → Подкатегория → Тип
- Галерея изображений с поддержкой видео
- Цена товара с форматированием
- Кнопка избранного
- Описание товара
- Характеристики товара (динамические поля)
- Карта с расположением товара (ProductMap на Leaflet)
- Карточка продавца (SellerCard) с ФИО, рейтингом, телефоном
- Форма отправки отзыва о продавце (ReviewForm)
- Кнопка скрытия/показа товара для владельца (ToggleProductButton)
- Ссылка на редактирование для владельца

### 4.5. Страница продавца

**Расположение:** `src/app/(feed)/seller/[sellerId]/`

**Рендеринг:** Серверный (SSR).

**Содержимое:**

- Информация о продавце (SellerInfo)
- Лента товаров продавца (SellerProductFeed)

### 4.6. Модуль личного кабинета

**Расположение:** `src/app/profile/`

**Защита:** Middleware + cookie `session_id`.

#### 4.6.1. Создание объявления (`/profile/create-product`)

- Форма создания товара с полями: название, цена, описание, состояние (новый/б/у)
- Выбор категории → подкатегории → типа (каскадный)
- Динамические поля характеристик (загружаются в зависимости от типа товара)
- Загрузка изображений (множественный выбор)
- Ссылка на видео
- Выбор адреса на интерактивной карте Leaflet
- Валидация всех полей, отправка через FormData

#### 4.6.2. Мои объявления (`/profile/my-products`)

- Список всех товаров текущего пользователя
- Управление: скрыть/показать, удалить
- Страница редактирования товара (`/profile/my-products/[productId]`)

#### 4.6.3. Сообщения (`/profile/messages`)

- Список всех чатов пользователя
- Информация о товаре в чате, последнее сообщение
- Счётчик непрочитанных сообщений
- Чат техподдержки (отдельная ссылка)
- Страница чата (`/profile/messages/[chatId]`) — realtime через Socket.IO
- Поддержка техподдержки (`/profile/messages/support`) — через localStorage

#### 4.6.4. Избранное (`/profile/favorites`)

- Список товаров, добавленных в избранное

#### 4.6.5. Настройки профиля (`/profile/settings`)

- Редактирование ФИО, номера телефона
- Загрузка и обрезка фото профиля (AvatarEditor)
- Выбор типа профиля (физ. лицо / ИП / ООО)
- Настройка «Принимаю звонки»

#### 4.6.6. Баланс (`/profile/balance`)

- Отображение текущего баланса и бонусного баланса
- Форма пополнения (интеграция с платёжной системой)
- Быстрые суммы пополнения ($100, $300, $500, $1000)
- История платежей (статусы: PENDING, CONFIRMED, COMPLETED, FAILED)
- Автоматическая проверка статуса ожидающих платежей каждые 30 сек
- Открытие ссылки оплаты в новой вкладке

#### 4.6.7. Аналитика (`/profile/analytics`)

- Статистика просмотров, лайков, сообщений
- Фильтр по периоду (год, месяц, неделя)
- Фильтр по категории
- Данные о количестве чатов и избранных

#### 4.6.8. Заявка на размещение баннера (`/profile/banner-request`)

- Форма заявки на размещение рекламного баннера
- Выбор места: лента товаров, профиль, чаты, избранное
- Загрузка изображения баннера
- Указание URL перехода и количества дней

#### 4.6.9. Статистика баннеров (`/profile/banner-stats`)

- Отображение статистики своих баннеров
- Общие просмотры, уникальные просмотры
- Разбивка по датам и местам размещения

#### 4.6.10. Продвижение товаров (`/profile/promotion-request`)

- Просмотр доступных типов продвижения с ценами за день
- Активация продвижения для конкретного товара

### 4.7. Модуль администрирования

**Расположение:** `src/app/admin/`

**Защита:** Middleware (cookie) + серверная проверка `checkIsAdmin()`. При отсутствии прав — 404.

#### 4.7.1. Модерация товаров (`/admin/moderation`)

- Список товаров в статусе модерации
- Одобрение (APPROVED) / отклонение (DENIED) с указанием причины
- Лента товаров с кнопками действий

#### 4.7.2. Модерация отзывов (`/admin/reviews`)

- Список отзывов на модерации
- Отображение рейтинга (звёзды), текста, информации об авторе
- Одобрение / отклонение отзыва

#### 4.7.3. Управление категориями (`/admin/categories`)

- Полный CRUD для иерархии: Категория → Подкатегория → Тип → Характеристика (Поле)
- Древовидный интерфейс с раскрывающимися уровнями
- Добавление, редактирование, удаление на каждом уровне

#### 4.7.4. Управление пользователями (`/admin/users`)

- Таблица всех пользователей
- Поиск по ID, ФИО, Email или телефону
- Действия: просмотр, блокировка/разблокировка, удаление, изменение баланса

#### 4.7.5. Системные логи (`/admin/logs`)

- Просмотр логов действий пользователей
- Сортировка по ID (новые сверху)
- Поиск по ID, действию, имени пользователя

#### 4.7.6. Управление рекламой (`/admin/advertising`)

- Управление рекламными баннерами на сайте
- Настройка мест размещения (лента товаров, профиль, чаты, избранное)

#### 4.7.7. Управление продвижением (`/admin/promotion`)

- Создание типов продвижения с ценой за день
- Активация продвижения для товаров

#### 4.7.8. Техподдержка (`/admin/support`)

- Список всех чатов пользователей
- Чат технической поддержки (через localStorage)
- WebSocket-интеграция для реалтайм-обновлений

### 4.8. Модуль чата (Realtime)

**Технология:** Socket.IO (WebSocket)

**Функциональность:**

- Создание чата при обращении к продавцу по конкретному товару
- Мгновенная доставка сообщений через WebSocket
- Отслеживание прочитанности сообщений
- Push-уведомления в браузере (Web Notifications API)
- Пагинация истории сообщений
- Техподдержка через localStorage (без серверной части)

### 4.9. Модуль избранного

**API:**

- `POST /favorite/add` — добавить товар в избранное
- `POST /favorite/remove` — удалить из избранного
- `GET /favorite` — получить все избранные товары

**UI:** Кнопка-сердечко (`LikeButton`) с оптимистичным обновлением состояния.

### 4.10. Модуль отзывов

- Отправка отзыва о продавце (текст + рейтинг 1–5 звёзд)
- Просмотр отзывов о продавце
- Модерация отзывов администратором

### 4.11. Модуль платежей

**Интеграция** с внешней платёжной системой:

- Создание платежа → получение URL для оплаты
- Проверка статуса платежа
- История платежей

### 4.12. Модуль рекламных баннеров

**Места размещения:** `PRODUCT_FEED` | `PROFILE` | `CHATS` | `FAVORITES`

**Функциональность:**

- CRUD баннеров (администратор)
- Трекинг просмотров баннеров
- Статистика: общие просмотры, уникальные просмотры, разбивка по датам
- Баннеры в ленте товаров, в чатах, в избранном, в профиле

### 4.13. Модуль геокодирования и адресов

- Подсказки адресов (`/address/suggestions`)
- Определение города по IP (`/address/city-by-ip`)
- Валидация адреса (`/address/validate`)
- Интерактивная карта Leaflet для выбора точки на карте

---

## 5. Модель данных (типы)

### 5.1. Пользователь (User)

```typescript
interface User {
  id: number;
  email?: string;
  fullName: string; // ФИО
  phoneNumber: string; // Телефон в формате +7...
  profileType: string; // "INDIVIDUAL" | "IP" | "OOO"
  rating: number | null; // Средний рейтинг по отзывам
  photo: string | null; // URL фото профиля
  balance: number; // Баланс в рублях
  bonusBalance?: number; // Бонусный баланс
  products?: number; // Кол-во товаров
  isBanned?: boolean; // Заблокирован
  usedFreeAds?: number; // Использованных бесплатных объявлений
  reviewsCount?: number; // Кол-во отзывов
  adsLimit?: {
    // Лимиты объявлений
    total: number;
    used: number;
    remaining: number;
    costPerAd: number;
  };
}
```

### 5.2. Товар (Product)

```typescript
interface Product {
  id: number;
  name: string; // Название
  address: string; // Адрес
  createdAt: string; // Дата создания
  images: string[]; // URL'ы изображений
  isFavorited: boolean; // В избранном у текущего пользователя
  isHide?: boolean; // Скрыт
  moderateState?: "APPROVED" | "DENIED" | "MODERATE"; // Статус модерации
  price: number; // Цена в рублях
  userId: number; // ID владельца
  videoUrl?: string | null; // Ссылка на видео
  hasPromotion?: boolean; // Продвигается
}

interface ExtendedProduct extends Product {
  description?: string; // Описание
  fieldValues?: Array<{ id: number; [key: string]: number | string }>; // Характеристики
  type: string | null; // Тип товара
  category: { id: number; name: string }; // Категория
  subCategory: { id: number; name: string }; // Подкатегория
  seller: {
    // Продавец
    id: number;
    fullName: string;
    phoneNumber: string;
    profileType: string;
    rating: number;
    reviewsCount: number;
  };
}
```

### 5.3. Категория (Category)

```typescript
interface Category {
  id: number;
  name: string;
  subCategories: Subcategory[];
}

interface Subcategory {
  id: number;
  name: string;
  subcategoryTypes: SubcategoryType[];
}

interface SubcategoryType {
  id: number;
  name: string;
  fields: Field[]; // Характеристики товаров этого типа
}

interface Field {
  id: number;
  name: string; // Название характеристики (напр. "Объём памяти")
}
```

### 5.4. Чат и сообщения

```typescript
interface Chat {
  id: number;
  createdAt: string;
  lastActivity: string;
  unreadCount?: number;
  companion: {
    id: number;
    fullName: string;
    phoneNumber: string;
    avatar?: string;
  };
  lastMessage?: {
    content: string;
    createdAt: string;
    isFromMe: boolean;
    isRead: boolean;
  };
  product: { id: number; name: string; image: string; price: number };
}

interface Message {
  id: number;
  chatId?: number;
  content?: string;
  createdAt: string;
  isFromMe?: boolean;
  isRead: boolean;
  senderId: number;
  sender?: { id: number; fullName: string };
}
```

### 5.5. Платёж (Payment)

```typescript
interface Payment {
  id: number;
  amount: number;
  status: "PENDING" | "CONFIRMED" | "COMPLETED" | "FAILED";
  paymentUrl: string;
  orderId: string;
  paymentId: string;
  userId: number;
  createdAt: string;
  updatedAt: string;
}
```

### 5.6. Баннер (Banner)

```typescript
interface Banner {
  id: number;
  photoUrl: string;
  place: "PRODUCT_FEED" | "PROFILE" | "CHATS" | "FAVORITES";
  name: string;
  navigateToUrl: string;
  userId: number;
  createdAt: string;
  updatedAt: string;
}
```

### 5.7. Лог (Log)

```typescript
interface Log {
  action: string; // Действие
  id: number;
  userId: number;
  user?: { email?: string; fullName: string; id: number };
}
```

---

## 6. UI-компоненты

### 6.1. Библиотека компонентов (shadcn/ui)

Проект использует shadcn/ui (стиль «New York») с компонентами на базе Radix UI:

| Компонент          | Назначение                                                                  |
| ------------------ | --------------------------------------------------------------------------- |
| **Button**         | Кнопки с вариантами (default, destructive, outline, secondary, ghost, link) |
| **Input**          | Текстовые поля ввода                                                        |
| **Textarea**       | Многострочный текст                                                         |
| **Dialog**         | Модальные окна (Radix Dialog)                                               |
| **Sheet**          | Выдвижные панели                                                            |
| **Select**         | Выпадающие списки (Radix Select)                                            |
| **Avatar**         | Аватары пользователей (Radix Avatar)                                        |
| **Badge**          | Бейджи / метки                                                              |
| **Breadcrumb**     | Хлебные крошки                                                              |
| **Typography**     | Типографика (h1–h4, p, muted и др.)                                         |
| **Logo**           | Логотип приложения                                                          |
| **Sonner/Toaster** | Toast-уведомления                                                           |
| **PhoneField**     | Поле ввода телефона с форматированием                                       |
| **ImageUpload**    | Загрузка изображений                                                        |
| **AddressMap**     | Карта для выбора адреса                                                     |

### 6.2. Бизнес-компоненты

| Компонент             | Назначение                                             |
| --------------------- | ------------------------------------------------------ |
| **ProductCard**       | Карточка товара в ленте                                |
| **MyProductCard**     | Карточка товара в личном кабинете                      |
| **ProductPreview**    | Превью изображений товара                              |
| **FeedWrapper**       | Грид-обёртка для ленты товаров                         |
| **LikeButton**        | Кнопка избранного                                      |
| **AuthDialog**        | Диалог авторизации (вход/регистрация/восстановление)   |
| **MainLayout**        | Основной layout (Header + Footer + Toaster)            |
| **Header**            | Шапка с логотипом, поиском, навигацией, мобильным меню |
| **Footer**            | Подвал сайта                                           |
| **ProductFeedBanner** | Рекламный баннер в ленте                               |
| **WideBanner**        | Широкий баннер                                         |
| **ChatsBanner**       | Баннер в чатах                                         |
| **HeroBanner**        | Главный баннер                                         |

---

## 7. Конфигурация и настройки

### 7.1. Next.js конфигурация (`next.config.ts`)

```typescript
{
  reactStrictMode: true,         // Строгий режим React
  output: "standalone",          // Standalone-сборка для Docker
  typedRoutes: true,            // Типизированные маршруты
  images: {
    unoptimized: true,          // Изображения без оптимизации Next.js
    remotePatterns: [{ protocol: "http", hostname: "localhost" }]
  }
}
```

### 7.2. TypeScript (`tsconfig.json`)

- Целевая платформа: ES2017
- Path aliases:
  - `@/*` → `./src/*`
  - `@/api/*` → `./src/lib/api/*`
- Строгий режим (strict: true)
- Инкрементальная компиляция

### 7.3. ESLint

- Основа: `@antfu/eslint-config` с поддержкой React, Next.js, JSX a11y
- Плагин TanStack Query для правил использования React Query
- Perfectionist: автоматическая сортировка импортов и массивов

### 7.4. Prettier

- Интеграция с `prettier-plugin-tailwindcss` для сортировки Tailwind-классов

### 7.5. CSS-переменные и тема

Цветовая палитра определена через CSS-переменные в `:root`:

| Переменная      | Значение                    | Назначение             |
| --------------- | --------------------------- | ---------------------- |
| `--primary`     | `#467bef`                   | Основной синий цвет    |
| `--secondary`   | `#17b95c`                   | Вторичный зелёный цвет |
| `--pink`        | `#f21957`                   | Акцентный розовый      |
| `--background`  | `#fff`                      | Фон                    |
| `--foreground`  | `#0a0a0a`                   | Текст                  |
| `--destructive` | oklch(0.577, 0.245, 27.325) | Цвет ошибок            |

Тёмная тема подготовлена (CSS-переменные `.dark` определены, но закомментированы).

---

## 8. Организационная структура проекта (компания)

На основании диаграммы `architecture-diagram.puml`, организация «Торгуй сам» имеет следующую структуру:

1. **Генеральный директор**
2. **Отдел разработки** — руководитель, CTO, фронтенд-разработчик, бэкенд-разработчик, DevOps-инженер, тестировщик
3. **Отдел модерации и контента** — руководитель, старший модератор, модератор объявлений, модератор отзывов, специалист по категориям
4. **Административный отдел** — руководитель, сисадмин, аналитик данных, специалист по безопасности, бухгалтер
5. **Отдел продаж и маркетинга** — руководитель, менеджер по рекламе, специалист по продвижению, аналитик продаж, контент-маркетолог
6. **Служба поддержки пользователей** — руководитель, старший специалист, поддержка продавцов, поддержка покупателей, техническая поддержка

---

## 9. REST API — карта эндпоинтов

### 9.1. Аутентификация (`/auth`)

| Метод | Эндпоинт                            | Описание                          |
| ----- | ----------------------------------- | --------------------------------- |
| POST  | `/auth/sign-in`                     | Авторизация                       |
| POST  | `/auth/sign-up?where=sms\|telegram` | Регистрация                       |
| POST  | `/auth/verify-mobile-code`          | Подтверждение SMS-кода            |
| POST  | `/auth/forgot-password`             | Запрос на восстановление пароля   |
| POST  | `/auth/verify-code`                 | Подтверждение кода восстановления |
| POST  | `/auth/change-password`             | Смена пароля                      |
| GET   | `/auth/me`                          | Текущий пользователь              |
| GET   | `/auth/isAdmin`                     | Проверка прав администратора      |

### 9.2. Товары (`/product`)

| Метод  | Эндпоинт                          | Описание                   |
| ------ | --------------------------------- | -------------------------- |
| GET    | `/product/all-products`           | Все товары (с фильтрами)   |
| GET    | `/product/search`                 | Поиск товаров              |
| GET    | `/product/available-filters`      | Доступные фильтры          |
| GET    | `/product/{id}`                   | Товар по ID                |
| GET    | `/product/random-products`        | Случайные товары           |
| GET    | `/product/products-to-moderate`   | Товары на модерации        |
| GET    | `/product/user-products/{userId}` | Товары пользователя        |
| POST   | `/product/create`                 | Создание товара (FormData) |
| PUT    | `/product/update/{id}`            | Обновление товара          |
| DELETE | `/product/delete/{id}`            | Удаление товара            |
| PUT    | `/product/moderate/{id}`          | Модерация товара           |
| PUT    | `/product/toggle/{id}`            | Скрыть/показать товар      |

### 9.3. Категории (`/category`)

| Метод | Эндпоинт                    | Описание            |
| ----- | --------------------------- | ------------------- |
| GET   | `/category/find-all`        | Все категории       |
| GET   | `/category/{id}`            | Категория по ID     |
| GET   | `/category/slug/{slug}`     | Категория по slug   |
| GET   | `/category/path/{slugPath}` | Полный путь по slug |

### 9.4. Пользователи (`/user`)

| Метод  | Эндпоинт                   | Описание                       |
| ------ | -------------------------- | ------------------------------ |
| GET    | `/user/find-all`           | Все пользователи (админ)       |
| GET    | `/user/info/{id}`          | Информация о пользователе      |
| GET    | `/user/me`                 | Текущий пользователь           |
| PATCH  | `/user/update-settings`    | Обновление настроек (FormData) |
| PUT    | `/user/set-balance`        | Установка баланса (админ)      |
| PUT    | `/user/toggle-banned/{id}` | Бан/разбан (админ)             |
| DELETE | `/user/delete/{id}`        | Удаление (админ)               |

### 9.5. Чаты (`/chat`)

| Метод | Эндпоинт              | Описание                   |
| ----- | --------------------- | -------------------------- |
| POST  | `/chat/start`         | Создать/открыть чат        |
| GET   | `/chat`               | Все чаты пользователя      |
| GET   | `/chat/{id}`          | Чат по ID                  |
| GET   | `/chat/{id}/messages` | Сообщения чата (пагинация) |

### 9.6. Избранное (`/favorite`)

| Метод | Эндпоинт           | Описание              |
| ----- | ------------------ | --------------------- |
| POST  | `/favorite/add`    | Добавить в избранное  |
| POST  | `/favorite/remove` | Удалить из избранного |
| GET   | `/favorite`        | Все избранные         |

### 9.7. Платежи (`/payment`)

| Метод | Эндпоинт                | Описание         |
| ----- | ----------------------- | ---------------- |
| POST  | `/payment/create`       | Создать платёж   |
| GET   | `/payment/check-status` | Проверить статус |
| GET   | `/payment/history`      | История платежей |

### 9.8. Баннеры (`/banner`)

| Метод  | Эндпоинт            | Описание                    |
| ------ | ------------------- | --------------------------- |
| POST   | `/banner`           | Создать баннер (FormData)   |
| GET    | `/banner`           | Получить баннеры (по месту) |
| PUT    | `/banner/{id}`      | Обновить баннер             |
| DELETE | `/banner/{id}`      | Удалить баннер              |
| POST   | `/banner/{id}/view` | Трекинг просмотра           |
| GET    | `/banner/stats`     | Статистика баннеров         |

### 9.9. Отзывы (`/review`)

| Метод | Эндпоинт                          | Описание            |
| ----- | --------------------------------- | ------------------- |
| POST  | `/review/send-review`             | Отправить отзыв     |
| GET   | `/review/all-reviews`             | Все отзывы          |
| GET   | `/review/user-reviews/{userId}`   | Отзывы пользователя |
| GET   | `/review/all-reviews-to-moderate` | Отзывы на модерации |
| PUT   | `/review/moderate-review/{id}`    | Модерация отзыва    |

### 9.10. Продвижение (`/promotion`)

| Метод | Эндпоинт                    | Описание                        |
| ----- | --------------------------- | ------------------------------- |
| GET   | `/promotion/all-promotions` | Типы продвижения                |
| POST  | `/promotion/create`         | Создать тип продвижения (админ) |
| POST  | `/promotion/add-promotion`  | Активировать продвижение        |

### 9.11. Адреса (`/address`)

| Метод | Эндпоинт               | Описание          |
| ----- | ---------------------- | ----------------- |
| GET   | `/address/suggestions` | Подсказки адресов |
| GET   | `/address/city-by-ip`  | Город по IP       |
| GET   | `/address/validate`    | Валидация адреса  |

### 9.12. Аналитика и логи

| Метод | Эндпоинт               | Описание         |
| ----- | ---------------------- | ---------------- |
| GET   | `/statistics/analytic` | Аналитика        |
| GET   | `/log/all`             | Все логи (админ) |

---

## 10. Ключевые паттерны и подходы

### 10.1. Feature-Sliced Design (упрощённый)

Проект частично следует Feature-Sliced Design:

- `_features/` — feature-специфичные модули (feed, category, ad, favorites, product)
- `_components/` — переиспользуемые компоненты уровня страницы
- `_lib/` — утилиты уровня модуля
- Подчёркивание в имени (`_`) обозначает приватные директории, исключённые из маршрутизации Next.js

### 10.2. Server Components + Client Components

- **Server Components** (по умолчанию): layout'ы, страницы товаров и продавцов — SSR с данными, загруженными на сервере
- **Client Components** (`"use client"`): интерактивные страницы — формы, чаты, админ-панель, компоненты с состоянием

### 10.3. SSR Prefetching + Hydration

- В корневом `layout.tsx` выполняется `prefetchQuery` для текущего пользователя
- `HydrationBoundary` с `dehydrate(queryClient)` передаёт серверные данные клиенту
- Это обеспечивает мгновенное отображение без «мигания» при загрузке

### 10.4. Оптимистичные обновления

- `LikeButton` обновляет состояние мгновенно, откатывая при ошибке

### 10.5. URL-state синхронизация

- Библиотека `nuqs` обеспечивает типобезопасную синхронизацию фильтров с URL query-параметрами
- Позволяет делиться ссылкой с примененными фильтрами

### 10.6. Централизованная обработка ошибок

- HTTP-клиент автоматически сбрасывает авторизацию при 401
- React Query с `retry: false` и кастомным `throwOnError`
- Глобальный `staleTime: 60s` для оптимизации запросов

---

## 11. Роли пользователей

| Роль              | Описание                      | Доступ                                                                                                            |
| ----------------- | ----------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| **Гость**         | Неавторизованный пользователь | Просмотр ленты, поиск, просмотр товаров и продавцов                                                               |
| **Пользователь**  | Авторизованный пользователь   | Размещение товаров, чат, избранное, баланс, аналитика, настройки, продвижение, баннеры                            |
| **Администратор** | Пользователь с правами админа | Модерация товаров и отзывов, управление категориями, пользователями, логами, рекламой, продвижением, техподдержка |

**Типы профилей пользователей:**

- `INDIVIDUAL` — физическое лицо
- `IP` — индивидуальный предприниматель
- `OOO` — общество с ограниченной ответственностью

---

## 12. Бизнес-процессы

### 12.1. Жизненный цикл объявления

1. Пользователь создаёт объявление (заполняет форму, выбирает категорию, загружает фото)
2. Объявление переходит в статус `MODERATE` (на модерации)
3. Администратор одобряет (`APPROVED`) или отклоняет (`DENIED`) с указанием причины
4. Одобренные объявления появляются в ленте и доступны для поиска
5. Владелец может скрыть (`isHide: true`) / показать / удалить / отредактировать объявление

### 12.2. Процесс покупки/коммуникации

1. Покупатель находит товар через поиск/каталог
2. Нажимает «Написать продавцу» → создаётся чат, привязанный к товару
3. Обмен сообщениями в реальном времени через WebSocket
4. Push-уведомления о новых сообщениях

### 12.3. Процесс пополнения баланса

1. Пользователь вводит сумму пополнения
2. Создаётся платёж → получение URL платёжной системы
3. Перенаправление на страницу оплаты
4. Автоматическая проверка статуса каждые 30 секунд
5. При успешной оплате → баланс обновляется

### 12.4. Процесс продвижения

1. Администратор создаёт типы продвижения (с ценой за день)
2. Пользователь выбирает тип продвижения и товар
3. Указывает количество дней → списание с баланса
4. Продвигаемые товары (`hasPromotion: true`) выделяются в ленте

### 12.5. Процесс размещения рекламного баннера

1. Пользователь подаёт заявку (изображение, URL, место, период)
2. Администратор создаёт/управляет баннерами
3. Баннеры отображаются в указанных местах (лента, профиль, чаты, избранное)
4. Система отслеживает просмотры (общие и уникальные)
5. Пользователь видит статистику своих баннеров

---

## 13. Нефункциональные требования

### 13.1. Производительность

- SSR для критичных страниц (товары, продавцы) — быстрая первичная загрузка
- `staleTime: 60s` для кэширования данных React Query
- Standalone-сборка Next.js — минимальный размер продакшн-бандла
- Многоступенчатая Docker-сборка — минимальный размер образа
- Unoptimized images — делегирование оптимизации CDN/бэкенду

### 13.2. Безопасность

- Сессионная аутентификация через HttpOnly cookie (`session_id`)
- Middleware-защита на уровне маршрутов
- Серверная проверка прав администратора
- Автоматический logout при 401

### 13.3. Доступность (Accessibility)

- Radix UI — ARIA-совместимые компоненты
- ESLint JSX a11y — автоматическая проверка доступности
- Кнопка избранного с `aria-label`
- Семантическая разметка (`article`, `main`, `header`, `footer`)

### 13.4. Адаптивность

- Полная адаптивность под мобильные устройства
- Мобильное меню в шапке (`MobileMenu`)
- Мобильная шапка в админ-панели (`MobileHeader`)
- Tailwind responsive утилиты (`sm:`, `md:`, `lg:`)

### 13.5. Масштабируемость

- Модульная архитектура с разделением по фичам
- Независимые React Query хуки для каждой сущности
- Переиспользуемые UI-компоненты
- Docker-контейнеризация для горизонтального масштабирования

---

## 14. Переменные окружения

| Переменная               | Назначение                      |
| ------------------------ | ------------------------------- |
| `NEXT_PUBLIC_API_URL`    | URL REST API бэкенда            |
| `NEXT_PUBLIC_API_WS_URL` | URL WebSocket-сервера для чатов |

---

## 15. Сборка и запуск

### Разработка

```bash
bun dev          # или npm run dev
```

### Сборка

```bash
bun run build    # или npm run build
```

### Продакшн

```bash
bun run start    # или npm run start
```

### Docker

```bash
docker-compose up --build
```

Приложение будет доступно на порту 3001.

### Линтинг и форматирование

```bash
bun run lint     # ESLint с автоисправлением
bun run format   # Prettier
```

---

## 16. Структура файлов проекта (полная)

```
trade-by-yourself/
├── .env                          # Переменные окружения (не в Git)
├── architecture-diagram.puml     # PlantUML-диаграмма организационной структуры
├── components.json               # Конфигурация shadcn/ui
├── docker-compose.yml            # Оркестрация Docker
├── Dockerfile                    # Многоступенчатая Docker-сборка
├── eslint.config.js              # Конфигурация ESLint
├── FILTERS_DOCUMENTATION.md      # Документация фильтров
├── middleware.ts                  # Next.js middleware (защита маршрутов)
├── next.config.ts                # Конфигурация Next.js
├── package.json                  # Зависимости и скрипты
├── postcss.config.js             # PostCSS конфигурация
├── prettier.config.js            # Конфигурация Prettier
├── tsconfig.json                 # Конфигурация TypeScript
│
├── public/                       # Статические файлы
│
└── src/
    ├── app/                      # Next.js App Router — страницы и layout'ы
    │   ├── globals.css           # Глобальные стили, CSS-переменные, тема
    │   ├── layout.tsx            # Корневой layout (SSR prefetch, шрифт)
    │   ├── providers.tsx         # Клиентские провайдеры
    │   ├── not-found.tsx         # 404 страница
    │   ├── _components/          # Общие layout-компоненты (Header, Footer)
    │   ├── (feed)/               # Публичная часть
    │   ├── profile/              # Личный кабинет
    │   └── admin/                # Админ-панель
    │
    ├── components/               # Переиспользуемые компоненты
    │   ├── ui/                   # Базовые UI-компоненты (shadcn/ui)
    │   ├── auth-dialog/          # Диалог авторизации
    │   ├── product-card/         # Карточка товара
    │   ├── feed-wrapper/         # Грид ленты товаров
    │   ├── like-button/          # Кнопка избранного
    │   ├── product-feed-banner/  # Рекламные баннеры
    │   └── _deprecated/          # Устаревшие компоненты
    │
    ├── lib/                      # Утилиты и сервисы
    │   ├── api/                  # API-слой (instance, requests, hooks, types)
    │   ├── contexts/             # React Context (auth, chat)
    │   ├── format.ts             # Форматирование (цена, ФИО)
    │   ├── phone.ts              # Работа с номерами телефонов
    │   ├── utils.ts              # Утилита cn() (clsx + tailwind-merge)
    │   ├── support-chat.ts       # Чат техподдержки (localStorage)
    │   ├── get-query-client.ts   # Фабрика QueryClient (SSR-safe)
    │   └── is-server.ts          # Определение среды выполнения
    │
    └── types/                    # TypeScript-типы (Product, User, Chat, Category, Payment, Log)
```

---

## 17. Резюме

«Торгуй сам» — это полнофункциональный веб-маркетплейс, построенный на современном стеке технологий (Next.js 16, React 19, TypeScript) с серверным и клиентским рендерингом. Приложение реализует классическую функциональность доски объявлений: размещение товаров с иерархической категоризацией, мощную систему поиска и фильтрации, real-time чат между покупателями и продавцами, систему отзывов, монетизацию через систему пополнения баланса и рекламные баннеры, а также полноценную административную панель для управления контентом и пользователями.

Проект контейнеризирован с помощью Docker, использует многоступенчатую сборку для оптимизации production-образа и развёртывается через Docker Compose. Frontend взаимодействует с отдельным REST API бэкендом и WebSocket-сервером для обеспечения реалтайм-коммуникаций.
